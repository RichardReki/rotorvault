{
  "name": "rotorvault-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "RotorVault off-chain agent: live signal -> rebalance plan (dry-run) + FDC Web2Json APY pipeline.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "agent": "tsx src/index.ts"
  },
  "dependencies": {
    "viem": "^2.21.45"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.2"
  }
}
